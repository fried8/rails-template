import React from 'react'
import { Route, Switch } from 'react-router'
import PageIndex from './containers/PageIndex'

export default (
  <Switch>
    {/*<Route exact path={window.apiUrl.FLIGHTS_INDEX} component={PageIndex} />
    <Route
      exact
      path={`${window.apiUrl.FLIGHTS_INDEX}/r`}
      component={PageResult}
    />*/}
  </Switch>
)
